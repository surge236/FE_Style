using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewGameButton_Control : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    // Checks if a folder has been made for save files, if not, prompts the user to create one.

    // Controls button action on press. This button will bring up a prompt for save file information
    // on button press.

    // After all save file creation has been completed, this method will switch to the initial game scene.

}
